<div class="bg-gray-100 rounded-lg p-6">
  <p class="mb-4">Olá <%= @resource.email %>!</p>
  <p class="mb-4">Este email é para confirmar que você alterou seu endereço de email para <%= @resource.unconfirmed_email %> em <%= Time.now.strftime("%d/%m/%Y às %H:%M") %>.</p>
  <p class="mb-4">Clique no botão abaixo para confirmar seu novo endereço de email:</p>
  <%= link_to 'Confirmar meu email', confirmation_url(@resource, confirmation_token: @token), class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline' %>
  <p class="mt-4">Obrigado,</p>
  <p><%= mailer_name.capitalize %></p>
</div>
